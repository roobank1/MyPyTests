
### Option B: If you already have dependencies in another file/location, update the workflow
If your project uses `pyproject.toml` (Poetry/PEP 621) or a subfolder, change the install step accordingly.

**If using `pyproject.toml` with pip:**
```yaml
- name: Install dependencies
  run: |
    python -m pip install --upgrade pip
    pip install -e .